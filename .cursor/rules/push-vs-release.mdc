---
description: Distinguish "push" (commit+push) from "release" (npm run release:*)
alwaysApply: true
---

# Push vs Release

In this repo these words are **not** interchangeable.

## «Запушить» / push / commit

Only:

1. `git add` relevant files
2. `git commit` (ask first if the user did not explicitly request a commit)
3. `git push` to the current branch

Do **not** bump version, build ZXP, tag, or upload to CDN.

## «Релиз» / release

Run the project release script for the requested variant — nothing more elaborate unless asked:

```bash
npm run release:patch
npm run release:minor
npm run release:major
npm run release:beta
npm run release:all          # Spunkram + Gal, independent versions / tags
npm run release:gal          # Gal only
npm run release:spunkram     # Spunkram only (default for release / release:patch)
```

Pass brand with patch/minor when needed:

```bash
npm run release:patch -- --brand=all
npm run release:patch -- --brand=gal
```

That script already bumps version (if asked), builds ZXP(s), commits, pushes, tags, and uploads per brand.

- If the user says only «зарелизь минор» → `npm run release:minor` (Spunkram)
- If they say «релиз для gal и spunkram» → `npm run release:all` (or `release:patch -- --brand=all`)
- If they say «закоммить и запушь» → commit + push only
- If they say both («запушь и зарелизь») → do commit+push of WIP only if needed, then `npm run release:…` (release may commit again if dirty)

Do not invent a long manual release pipeline when `npm run release:*` exists.
