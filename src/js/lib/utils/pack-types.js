import { PACKAGE_FILE_EXTENSIONS } from "@brands";
/** Pack file extensions accepted from Motionflow Market. */
export const PACKAGE_FILETYPES = [...PACKAGE_FILE_EXTENSIONS];
/** Join char for category instance paths (legacy: Typography-@-Basic). */
export const INSTANCE_GROUP_JOIN_CHAR = "-@-";
export const PACK_ASSETS_FOLDER = "Previews";
